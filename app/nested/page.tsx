'use client';
export default function Nested() {
    return (
        <main>
            THIS IS NESTED PAGE. <button onClick={() => window.location.reload()}>RELOAD ME</button>
        </main>
    );
}
